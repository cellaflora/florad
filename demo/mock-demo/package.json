{
  "name": "mock-demo",
  "version": "0.1.0",
  "description": "",
  "main": "flora-config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "flora build",
    "deploy": "flora deploy",
    "publish": "flora publish",
    "publish:dev": "flora publish dev"
  },
  "author": "Cellaflora (dev@cellaflora.com)",
  "license": "UNLICENSED",
  "dependencies": {
    "flora": "file:../.."
  }
}
