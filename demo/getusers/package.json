{
  "name": "getusers",
  "version": "0.2.0",
  "description": "",
  "main": "userapi.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "flora build",
    "deploy": "flora deploy",
    "publish": "flora publish",
    "publish:dev": "flora publish dev"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "flora": "file:../.."
  }
}
